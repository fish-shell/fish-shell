# The fish_mode_prompt function is prepended to the prompt
function fish_mode_prompt --description "Displays the current mode"
    # by default it calls the default mode prompt
    fish_default_mode_prompt
end
