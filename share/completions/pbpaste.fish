complete -c pbpaste -s help -d "Display command help"
complete -c pbpaste -s pboard -d "Specifies which paste specifies which pasteboard to copy to or paste from. If no pasteboard is given, the general pasteboard will be used by default. {general | ruler | find | font}"
complete -c pbpaste -s Prefer -d "Tells pbpaste what type of data to look for in the pasteboard first. {txt | rtf | ps}"
