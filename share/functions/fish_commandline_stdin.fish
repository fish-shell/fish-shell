# Pipe data to commandline
function fish_commandline_stdin
    read -l line; commandline -a $line
end
# function clia
    # read -l line; commandline -a $line
# end
# function to_buffer
   # read -l line; commandline -a $line
# end
